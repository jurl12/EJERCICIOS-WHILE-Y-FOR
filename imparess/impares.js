let numeros = parseInt(prompt("Ingrese hasta que numero impar muestre"))
let impar;
for (let index = 0; index <= numeros ; index++) {
    if (index % 2 != 0) {
        console.log(index);
    }
    
}